<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        <link href="{{ asset('css/main.css') }}" type="text/css" rel="stylesheet" />
        <link href="{{ asset('bootstrap/css/bootstrap.css') }}" type="text/css" rel="stylesheet" />
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body>
<header>
    <div class="container">
        <div class="col-lg-12">
        <div id="signup">
            {% if app.user %}
                <u>User</u> {{ app.user.username  }} |<a href="{{ path('logout') }}">Déconnexion</a>
            {% else %}
                <a href="{{ path('register') }}">Inscription</a> | <a href="{{ path('login') }}">Connexion</a>
            {% endif %}
        </div>
        </div>
    </div>
</header>
{% block body %}{% endblock %}
<footer>

</footer>
{% block javascripts %}
    <script src="{{ asset('js/jquery.js') }}"></script>
    <script src="{{ asset('bootstrap/js/bootstrap.js') }}"></script>
{% endblock %}
</body>
</html>
